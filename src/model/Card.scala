package model

class Card(symbol:String, value:Integer, name:String) {
  /*
  private var symbol:String = "";
  private var wert:Integer  = 0
  
  def setSymbol(symbol:String) = this.symbol = symbol
  
  def setWert(wert:Integer) = this.wert = wert
  */
  
}